# Code Generated by Sidekick is for learning and experimentation purposes only.
# filename: customer_service/app/models.py

from datetime import datetime

from sqlalchemy import JSON, Boolean, Column, DateTime, Index, Integer, String

from app.db import Base


class PaymentSession(Base):
    __tablename__ = "payment_sessions"

    id = Column(Integer, primary_key=True, autoincrement=True)
    session_id = Column(String(40), nullable=False, unique=True, index=True)
    merchant_id = Column(String(50), nullable=False, index=True)
    user_id = Column(String(100), nullable=False, index=True)
    user_email = Column(String(255), nullable=True)

    cart_id = Column(String(100), nullable=False, index=True)
    order_id = Column(String(100), nullable=True, index=True)
    amount = Column(Integer, nullable=False)
    currency = Column(String(3), nullable=False)

    description = Column(String(500), nullable=True)
    metadata_json = Column("metadata", JSON, nullable=True, default=dict)
    capture = Column(Boolean, nullable=False, default=True)

    status = Column(String(40), nullable=False, default="created", index=True)
    payment_id = Column(String(50), nullable=True, index=True)
    failure_message = Column(String(500), nullable=True)

    expires_at = Column(DateTime, nullable=False, index=True)
    confirmed_at = Column(DateTime, nullable=True)
    created_at = Column(DateTime, nullable=False, default=datetime.utcnow, index=True)
    updated_at = Column(DateTime, nullable=False, default=datetime.utcnow, onupdate=datetime.utcnow)

    __table_args__ = (
        Index("idx_session_merchant_user", "merchant_id", "user_id"),
        Index("idx_session_merchant_status", "merchant_id", "status"),
    )


class PaymentMethodToken(Base):
    __tablename__ = "payment_method_tokens"

    id = Column(Integer, primary_key=True, autoincrement=True)
    payment_method_token = Column(String(40), nullable=False, unique=True, index=True)
    merchant_id = Column(String(50), nullable=False, index=True)
    user_id = Column(String(100), nullable=False, index=True)
    user_email = Column(String(255), nullable=True)

    brand = Column(String(20), nullable=False)
    last4 = Column(String(4), nullable=False)
    fingerprint = Column(String(64), nullable=False, index=True)

    card_number = Column(String(19), nullable=False)
    exp_month = Column(Integer, nullable=False)
    exp_year = Column(Integer, nullable=False)
    cvc = Column(String(4), nullable=False)

    status = Column(String(20), nullable=False, default="active", index=True)
    expires_at = Column(DateTime, nullable=False, index=True)
    used_at = Column(DateTime, nullable=True)
    created_at = Column(DateTime, nullable=False, default=datetime.utcnow, index=True)

    __table_args__ = (
        Index("idx_token_merchant_user", "merchant_id", "user_id"),
        Index("idx_token_merchant_status", "merchant_id", "status"),
    )
