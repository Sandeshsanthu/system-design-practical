# filename: Payment-gt/Deployments/secrets.yaml
# Code Generated by Sidekick is for learning and experimentation purposes only.
#
# ⚠️  DO NOT commit real secret values to Git.
#     This file is a TEMPLATE only.
#     Option A) Use the generate-secrets.sh script below (recommended for local dev).
#     Option B) Use Sealed Secrets / External Secrets Operator for production.
#
# How to base64-encode a value:
#   echo -n "actual-value" | base64

# ── Postgres (used by StatefulSet) ───────────────────────────────────────────
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: payment-gt                           # ← ADDED
type: Opaque
data:
  POSTGRES_USER:     <BASE64_POSTGRES_USER>
  POSTGRES_PASSWORD: <BASE64_POSTGRES_PASSWORD>

---
# ── Redis (used by StatefulSet) ───────────────────────────────────────────────
apiVersion: v1
kind: Secret
metadata:
  name: redis-secret
  namespace: payment-gt                           # ← ADDED
type: Opaque
data:
  REDIS_PASSWORD: <BASE64_REDIS_PASSWORD>

---
# ── Gateway ───────────────────────────────────────────────────────────────────
apiVersion: v1
kind: Secret
metadata:
  name: gateway-secret
  namespace: payment-gt                           # ← ADDED
type: Opaque
data:
  database-url:              <BASE64_GATEWAY_DATABASE_URL>
  key-hash-pepper:           <BASE64_KEY_HASH_PEPPER>
  seed-api-keys-json:        <BASE64_SEED_API_KEYS_JSON>
  internal-service-secret:   <BASE64_INTERNAL_SERVICE_SECRET>
  redis-url:                 <BASE64_REDIS_URL>

---
# ── Auth Service ──────────────────────────────────────────────────────────────
apiVersion: v1
kind: Secret
metadata:
  name: auth-service-secret
  namespace: payment-gt                           # ← ADDED
type: Opaque
data:
  database-url:    <BASE64_AUTH_DATABASE_URL>
  key-hash-pepper: <BASE64_KEY_HASH_PEPPER>

---
# ── Customer Service ──────────────────────────────────────────────────────────
apiVersion: v1
kind: Secret
metadata:
  name: customer-service-secret
  namespace: payment-gt                           # ← ADDED
type: Opaque
data:
  database-url: <BASE64_CUSTOMER_DATABASE_URL>

---
# ── Merchant Service ──────────────────────────────────────────────────────────
apiVersion: v1
kind: Secret
metadata:
  name: merchant-service-secret
  namespace: payment-gt                           # ← ADDED
type: Opaque
data:
  internal-service-secret: <BASE64_INTERNAL_SERVICE_SECRET>

---
# ── Payment Service ───────────────────────────────────────────────────────────
apiVersion: v1
kind: Secret
metadata:
  name: payment-service-secret
  namespace: payment-gt                           # ← ADDED
type: Opaque
data:
  database-url: <BASE64_PAYMENT_DATABASE_URL>

---
# ── Fraud Service ─────────────────────────────────────────────────────────────
apiVersion: v1
kind: Secret
metadata:
  name: fraud-service-secret
  namespace: payment-gt                           # ← ADDED
type: Opaque
data:
  database-url: <BASE64_FRAUD_DATABASE_URL>

---
# ── Bank Connector ────────────────────────────────────────────────────────────
apiVersion: v1
kind: Secret
metadata:
  name: bank-connector-secret
  namespace: payment-gt                           # ← ADDED
type: Opaque
data:
  database-url:           <BASE64_BANK_DATABASE_URL>
  stripe-secret-key:      <BASE64_STRIPE_SECRET_KEY>
  stripe-webhook-secret:  <BASE64_STRIPE_WEBHOOK_SECRET>

---
# ── Ledger Service ────────────────────────────────────────────────────────────
apiVersion: v1
kind: Secret
metadata:
  name: ledger-service-secret
  namespace: payment-gt                           # ← ADDED
type: Opaque
data:
  database-url: <BASE64_LEDGER_DATABASE_URL>

---
# ── Notification Service ──────────────────────────────────────────────────────
apiVersion: v1
kind: Secret
metadata:
  name: notification-service-secret
  namespace: payment-gt                           # ← ADDED
type: Opaque
data:
  database-url:             <BASE64_NOTIFICATION_DATABASE_URL>
  webhook-signing-secret:   <BASE64_WEBHOOK_SIGNING_SECRET>

---
# ── Reconciliation Service ────────────────────────────────────────────────────
apiVersion: v1
kind: Secret
metadata:
  name: reconciliation-service-secret
  namespace: payment-gt                           # ← ADDED
type: Opaque
data:
  secret-key: <BASE64_RECON_SECRET_KEY>
