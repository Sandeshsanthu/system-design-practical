# filename: Payment-gt/Deployments/customer-service.yaml
# Code Generated by Sidekick is for learning and experimentation purposes only.

apiVersion: apps/v1
kind: Deployment
metadata:
  name: customer-service-stable
  namespace: payment-gt
  labels:
    app: customer-service
    track: stable
spec:
  replicas: 3
  selector:
    matchLabels:
      app: customer-service
      track: stable
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: customer-service
        track: stable
    spec:
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              app: customer-service
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: customer-service
      terminationGracePeriodSeconds: 30
      containers:
        - name: customer-service
          image: sandeshs/payment-gt-customer-service:90c8c62b87c2ddc4c8a8fbdaefd5434eb5268a97
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8002
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: customer-service-secret
                  key: database-url
            - name: MERCHANT_SERVICE_URL
              valueFrom:
                configMapKeyRef:
                  name: payment-gt-config
                  key: MERCHANT_SERVICE_URL
            - name: CHECKOUT_SESSION_TTL_MINUTES
              valueFrom:
                configMapKeyRef:
                  name: payment-gt-config
                  key: CHECKOUT_SESSION_TTL_MINUTES
            - name: PAYMENT_METHOD_TTL_MINUTES
              valueFrom:
                configMapKeyRef:
                  name: payment-gt-config
                  key: PAYMENT_METHOD_TTL_MINUTES
            - name: PORT
              value: "8002"
          resources:
            requests:
              cpu: 150m
              memory: 256Mi
            limits:
              cpu: 400m
              memory: 512Mi
          startupProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 5
            failureThreshold: 24
          livenessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: customer-service-canary
  namespace: payment-gt
  labels:
    app: customer-service
    track: canary
spec:
  replicas: 1
  selector:
    matchLabels:
      app: customer-service
      track: canary
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: customer-service
        track: canary
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: customer-service
          image: sandeshs/payment-gt-customer-service:90c8c62b87c2ddc4c8a8fbdaefd5434eb5268a97
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8002
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: customer-service-secret
                  key: database-url
            - name: MERCHANT_SERVICE_URL
              valueFrom:
                configMapKeyRef:
                  name: payment-gt-config
                  key: MERCHANT_SERVICE_URL
            - name: CHECKOUT_SESSION_TTL_MINUTES
              valueFrom:
                configMapKeyRef:
                  name: payment-gt-config
                  key: CHECKOUT_SESSION_TTL_MINUTES
            - name: PAYMENT_METHOD_TTL_MINUTES
              valueFrom:
                configMapKeyRef:
                  name: payment-gt-config
                  key: PAYMENT_METHOD_TTL_MINUTES
            - name: PORT
              value: "8002"
            - name: CANARY
              value: "true"
          resources:
            requests:
              cpu: 150m
              memory: 256Mi
            limits:
              cpu: 400m
              memory: 512Mi
          startupProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 5
            failureThreshold: 24
          livenessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3

---
apiVersion: v1
kind: Service
metadata:
  name: customer-service
  namespace: payment-gt
spec:
  selector:
    app: customer-service
  ports:
    - name: http
      port: 8002
      targetPort: http
  type: ClusterIP

---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: customer-service-stable-hpa
  namespace: payment-gt
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: customer-service-stable
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
  behavior:
    scaleUp:
      stabilizationWindowSeconds: 60
      policies:
        - type: Pods
          value: 2
          periodSeconds: 60
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
        - type: Pods
          value: 1
          periodSeconds: 120

---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: customer-service-pdb
  namespace: payment-gt
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app: customer-service
      track: stable
