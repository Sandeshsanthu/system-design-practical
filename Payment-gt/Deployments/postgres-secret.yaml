# filename: Payment-gt/Deployments/postgres-secret.yaml
# Code Generated by Sidekick is for learning and experimentation purposes only.
#
# ⚠️  TEMPLATE — never commit real values to Git.
# Generate:  echo -n "your-value" | base64

apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: payment-gt
  labels:
    app: postgres
type: Opaque
data:
  POSTGRES_USER:     <BASE64_POSTGRES_USER>      # ← no plaintext here
  POSTGRES_PASSWORD: <BASE64_POSTGRES_PASSWORD>  # ← no plaintext here
