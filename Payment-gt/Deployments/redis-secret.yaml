# filename: Payment-gt/Deployments/redis-secret.yaml
# Code Generated by Sidekick is for learning and experimentation purposes only.
#
# ⚠️  TEMPLATE — never commit real values to Git.
# Generate:  echo -n "your-password" | base64
# Usage in app:  REDIS_URL=redis://:<password>@redis:6379/0

apiVersion: v1
kind: Secret
metadata:
  name: redis-secret
  namespace: payment-gt
  labels:
    app: redis
type: Opaque
data:
  REDIS_PASSWORD: <BASE64_REDIS_PASSWORD>    # ← no plaintext here
