# Code Generated by Sidekick is for learning and experimentation purposes only.
# filename: bank_connector/app/config.py

import os


class Settings:
    service_name: str = os.getenv("SERVICE_NAME", "bank_connector")
    port: int = int(os.getenv("PORT", "8005"))
    database_url: str = os.getenv(
        "DATABASE_URL",
        "postgresql://payments:payments@postgres:5432/bank_connector_db",
    )

    # ── Stripe ────────────────────────────────────────────────────────
    # Use sk_test_... for sandbox, sk_live_... for production
    stripe_secret_key: str = os.getenv("STRIPE_SECRET_KEY", "sk_test_placeholder")
    stripe_webhook_secret: str = os.getenv("STRIPE_WEBHOOK_SECRET", "whsec_placeholder")
    stripe_api_version: str = os.getenv("STRIPE_API_VERSION", "2024-06-20")

    # ── Retry ─────────────────────────────────────────────────────────
    max_retries: int = int(os.getenv("MAX_RETRIES", "3"))
    retry_wait_seconds: float = float(os.getenv("RETRY_WAIT_SECONDS", "1.0"))
    retry_max_wait_seconds: float = float(os.getenv("RETRY_MAX_WAIT_SECONDS", "10.0"))

    # ── Circuit Breaker ────────────────────────────────────────────────
    circuit_breaker_failure_threshold: int = int(os.getenv("CB_FAILURE_THRESHOLD", "5"))
    circuit_breaker_timeout_seconds: int = int(os.getenv("CB_TIMEOUT_SECONDS", "60"))

    # ── Timeouts ──────────────────────────────────────────────────────
    stripe_timeout_seconds: int = int(os.getenv("STRIPE_TIMEOUT_SECONDS", "30"))


settings = Settings()
