# Code Generated by Sidekick is for learning and experimentation purposes only.

from functools import lru_cache

from pydantic_settings import BaseSettings


class Settings(BaseSettings):
    # Service
    SERVICE_NAME: str = "notification-service"
    PORT: int = 8008
    DEBUG: bool = False

    # Database
    DATABASE_URL: str = "postgresql://payments:payments@postgres:5432/notification_db"

    # SendGrid
    SENDGRID_API_KEY: str = ""
    SENDGRID_FROM_EMAIL: str = "noreply@paymentgateway.dev"
    SENDGRID_FROM_NAME: str = "Payment Gateway"

    # Twilio
    TWILIO_ACCOUNT_SID: str = ""
    TWILIO_AUTH_TOKEN: str = ""
    TWILIO_FROM_NUMBER: str = ""

    # Webhook
    WEBHOOK_SIGNING_SECRET: str = "whsec_dev_secret_32_chars_minimum"
    WEBHOOK_TIMEOUT_SECONDS: int = 10
    WEBHOOK_MAX_RETRIES: int = 3
    WEBHOOK_RETRY_DELAYS: list[int] = [10, 30, 60]   # seconds between retries

    # Mock mode — set to True in dev to log instead of calling real APIs
    MOCK_EMAIL: bool = True
    MOCK_SMS: bool = True

    class Config:
        env_file = ".env"

@lru_cache
def get_settings() -> Settings:
    return Settings()
