# Code Generated by Sidekick is for learning and experimentation purposes only.
# filename: fraud_service/app/config.py

import os


class Settings:
    service_name: str = os.getenv("SERVICE_NAME", "fraud_service")
    database_url: str = os.getenv(
        "DATABASE_URL",
        "postgresql://payments:payments@postgres:5432/fraud_db",
    )
    port: int = int(os.getenv("PORT", "8004"))

    # Risk thresholds
    low_risk_threshold: int = int(os.getenv("LOW_RISK_THRESHOLD", "30"))
    medium_risk_threshold: int = int(os.getenv("MEDIUM_RISK_THRESHOLD", "70"))

    # Velocity limits
    card_velocity_window_minutes: int = int(os.getenv("CARD_VELOCITY_WINDOW_MINUTES", "60"))
    card_velocity_max_attempts: int = int(os.getenv("CARD_VELOCITY_MAX_ATTEMPTS", "5"))
    ip_velocity_window_minutes: int = int(os.getenv("IP_VELOCITY_WINDOW_MINUTES", "60"))
    ip_velocity_max_attempts: int = int(os.getenv("IP_VELOCITY_MAX_ATTEMPTS", "10"))
    customer_velocity_window_hours: int = int(os.getenv("CUSTOMER_VELOCITY_WINDOW_HOURS", "24"))
    customer_velocity_max_attempts: int = int(os.getenv("CUSTOMER_VELOCITY_MAX_ATTEMPTS", "10"))

    # ML model
    ml_model_enabled: bool = os.getenv("ML_MODEL_ENABLED", "true").lower() == "true"
    ml_model_weight: float = float(os.getenv("ML_MODEL_WEIGHT", "0.3"))
    rules_weight: float = float(os.getenv("RULES_WEIGHT", "0.7"))


settings = Settings()
