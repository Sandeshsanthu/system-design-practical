# Code Generated by Sidekick is for learning and experimentation purposes only.
# filename: ledger_service/app/config.py

import os


class Settings:
    service_name: str = os.getenv("SERVICE_NAME", "ledger_service")
    port: int = int(os.getenv("PORT", "8007"))
    database_url: str = os.getenv(
        "DATABASE_URL",
        "postgresql://payments:payments@postgres:5432/ledger_db",
    )

    # Platform fee percentage (e.g. 2.5 = 2.5%)
    platform_fee_percent: float = float(os.getenv("PLATFORM_FEE_PERCENT", "2.5"))

    # Reserved account IDs
    platform_account_id: str = os.getenv("PLATFORM_ACCOUNT_ID", "acc_platform")
    escrow_account_id: str = os.getenv("ESCROW_ACCOUNT_ID", "acc_escrow")
    fee_account_id: str = os.getenv("FEE_ACCOUNT_ID", "acc_fees")
    refund_reserve_account_id: str = os.getenv("REFUND_RESERVE_ACCOUNT_ID", "acc_refund_reserve")


settings = Settings()
